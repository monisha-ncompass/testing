version: 0.2

phases:
  build:
    commands:
      - npm -v
      - bash build/deploy.sh
      - sam package --template-file template.yaml --s3-bucket "m-codepipeline" --output-template-file packaged.yaml
artifacts:
  files:
    - "packaged.yaml"
  discard-paths: yes
